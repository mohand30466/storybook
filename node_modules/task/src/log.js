const chalk = require('chalk')

exports.log = {
  props: {
    default: {
      on: (val, stamp, t) => {
        if (val) {
          console.log(chalk.cyan(t.parent(2).key || 'task-hub'), t.compute().replace(/\n$/, ''))
        }
      }
    }
  }
}
