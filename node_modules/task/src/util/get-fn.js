module.exports = task => {
  if (task) {
    let fn = task.val
    let inherits = task.inherits
    while (!fn && inherits) {
      fn = inherits.val
      if (inherits === (inherits = inherits.inherits)) break
    }
    return fn
  }
}
