# task
Run tasks.

```js
task.set({
  tasks: {
    inject: [
      require('task/packages/task-build-js')
    ],
    'build-my-file': {
      type: 'build-js',
      options: {
        entry: 'my-file/index.js',
        dest: 'my-file/build.js'
      },
      watch: true
    }
  }
})
```
