'use strict'

// @TODO: fix tests so they don't block

const test = require('tape')

test.onFinish(() => process.exit())

// require('./done')
// require('./after')
// require('../packages/task-minify/test')

// /* while we have the packages in this repo */
// const fs = require('fs')
// fs.readdir('packages', (err, files) => {
//   if (err) return console.log(err)
//   files.forEach(file => {
//     if (file[0] !== '.') {
//       try {
//         require('../packages/' + file + '/test')
//       } catch (e) {
//         // no such thing
//       }
//     }
//   })
// })
