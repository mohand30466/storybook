// const fs = require('fs')
const test = require('tape')
const ploy = require('../../../')
// const { join } = require('path')
const dev = require('../')
// const http = require('http')

test.onFinish(() => process.exit())

test('simple rollup build (with cwd)', { timeout: 30000000 }, t => {
  ploy.set({
    cwd: __dirname,
    inject: [ dev ]
  })
  t.plan(2)
})
